---
import Base from "../../../layouts/Base.astro";
import ArticlesLibrary from "../../../components/ArticlesLibrary.astro";
import { getContent, normalizeLocale } from "../../../i18n/utils";
const locale = normalizeLocale(Astro.currentLocale);
const { articles } = getContent(locale);
---

<Base title={`${articles.title} | Ibson Junior`} description={articles.subtitle}>
  <ArticlesLibrary />
</Base>
